#pragma once

#include "main.h"

// Allows change lengths from controller
class Config {
    private:
        std::unordered_map<std::string, QLength> double_vars;
    public:
        void register_double
};